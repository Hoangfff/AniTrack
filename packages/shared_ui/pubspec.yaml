name: shared_ui
description: "AniTrack shared design system — colors, typography, spacing, theme, and reusable widgets."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
